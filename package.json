{
  "name": "probot-kredits",
  "version": "1.0.0",
  "description": "",
  "author": "Michael Bumann <hello@michaelbumann.com> (http://kredits.kosmos.org)",
  "license": "ISC",
  "repository": "https://github.com/67p/probot-kredits.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "ethers": "^3.0.15",
    "kredits-contracts": "github:67P/truffle-kredits",
    "probot": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
